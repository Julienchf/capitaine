# Copie ce fichier en ".env" et colle les valeurs de ton projet Supabase.
# Sans ces valeurs, l'app fonctionne en mode local (données sur ce seul appareil).
# Ces deux valeurs sont publiques (côté navigateur) — pas de secret ici.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
